1.创建conda环境 python=3.11，pip install -r requirements.txt

2.创建两个端口



端口1：

linux：

export OPENAI_API_KEY=sk-fq74UNBLU6pmRPtaZxtB8Nf6tkrfvBJosPyCloNWU4sf9KFx
export OPENAI_BASE_URL=https://xiaohumini.site/v1
export RETRIEVER=duckduckgo

python -m uvicorn main:app --host 0.0.0.0 --port 8001 --reload





端口2：

curl -X POST 'http://127.0.0.1:8001/report/' \
  -H 'Content-Type: application/json' \
  -d '{
    "task":"Why is Nvidia stock going up?",
    "report_type":"deep",
    "report_source":"web",
    "tone":"Objective",
    "headers":{},
    "repo_name":"",
    "branch_name":"",
    "generate_in_background": false
  }'



在gpt-researcher\outputs 目录下一次研究产生四个文件docx，pdf，json，md文件。

docx和pdf为生成的报告，json文件包含每个时间执行的操作，md文件包含任务结构图。